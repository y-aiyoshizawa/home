JavaSilverで分かったこと
---------------------------------------------------------------------
dobuleのゼロ割は割られるほうによって変化する
0 > infinity
0 = NaN
0 < -infinity
---------------------------------------------------------------------
static final int i;
iは実行される時に初期化されて、再代入不可能になる
---------------------------------------------------------------------
public static void main(String... v){}
可変長引数の存在を知った
---------------------------------------------------------------------
new String以外でStringを作ったかつ同じ文字の場合は
同じ個所を参照する
---------------------------------------------------------------------
=演算子は優先順位が低い
---------------------------------------------------------------------
i++の挙動

//iは毎回0としてtrueになるもの
i++ == 0
1 + i++ == 1
++i + i++ == 2
(i = 1) + i++ == 2 //
(i = 0) + i++ == 0 //この辺の挙動はおかしい

iの更新があった場合、==よりもi++が優先されるのかな？
---------------------------------------------------------------------
transient修飾子
オブジェクトをシリアライズするときに対象外になる
---------------------------------------------------------------------
オーバーロードする場合、名前が一致するかつ引数が違う場合にできる。
この時、戻り値が違っていても構わない
注意：戻り値だけ違う場合はコンパイルエラーになる。
---------------------------------------------------------------------
コンパイラは値を初期化しているか、していないかを見ている
String s;
s.toString() //コンパイルエラー

String s = null;
s.toString() //実行時エラー
---------------------------------------------------------------------
RuntimeException以外のExceptionは例外処理が必須
RuntimeExceptionは任意

---------------------------------------------------------------------